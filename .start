#!/bin/bash

# Trying to mount windows-ntfs drives:

sudo mount -t ntfs-3g /dev/sdb1 /mnt/data/
sudo mount -t ntfs-3g /dev/sda1 /mnt/stuff/
sudo mount -t ntfs-3g /dev/nvme0n1p3 /mnt/wins/
sudo mount -t ntfs-3g /dev/nvme0n1p6 /mnt/ssd_data/
